{
  "name": "cosmic-arc-prototype",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^8.1.0",
    "typescript": "^5.4.5"
  }
}
